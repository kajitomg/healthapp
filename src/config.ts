
const config = {
  store: {
    modules: {
      session: {
        tokenHeader: 'X-Token',
        languageHeader: 'Accept-Language'
      }
    }
  },
  api: {
    baseUrl: ''
  },
  redux: {
  
  }
}

export default config;
